syntax = "proto3";
package svc.user;
option go_package = "microsvc/protocol/svc/review";


// *Ext 是外部服务接口组，要鉴权
// *Int 是内部服务接口组，可不鉴权
service ReviewExt {
  // 接口名称不要过于笼统，这样不便于搜索
  // good：ReviewResource
  // bad: Review
}
